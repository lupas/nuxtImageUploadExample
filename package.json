{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@types/fs-extra": "^9.0.13",
    "nuxt": "3.0.0"
  },
  "dependencies": {
    "fs-extra": "^11.1.0"
  }
}
